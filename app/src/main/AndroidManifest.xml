<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.zhaoweihao.shopping">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".goods.GoodActivity" >

        </activity>
        <activity android:name=".trade.BuyActivity" />
        <activity android:name=".goods.CollectionActivity" />
        <activity android:name=".user.ConcernActivity" />
        <activity android:name=".trade.PurchaserTradeActivity" />
        <activity android:name=".trade.SellerTradeActivity" />
        <activity android:name=".user.UpdateUserInfoActivity" />
        <activity android:name=".goods.PublishGoodsActivity" />
        <activity android:name=".goods.MyGoodsActivity" />
        <activity android:name=".message.NotificationActivity" />
        <activity android:name=".ui.SearchActivity" >

        </activity>
        <activity android:name=".goods.UpdateGoodsActivity" />
        <activity android:name=".trade.CommentActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".user.AuthenticateActivity" />
        <activity android:name=".ui.SettingActivity" />
        <activity android:name=".message.ChatActivity">

        </activity>
    </application>

</manifest>